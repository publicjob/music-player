import styled from 'styled-components'

const SingerWrapper = styled.div`
  position: absolute;
  width: 100%;
  height: 100%;
  z-index: 3;
  color: #fff;
  background-color: #000000;
`
const SingerHeader = styled.div`
  margin: 10px 5px;
`
const SingerBody = styled.div`

`
export {
  SingerWrapper,
  SingerHeader, 
  SingerBody
}